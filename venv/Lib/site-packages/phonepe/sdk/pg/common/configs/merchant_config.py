from dataclasses import dataclass


@dataclass
class MerchantConfig:
    merchant_id: str
    salt_key: str
    salt_index: int

    def __post_init__(self):
        if self.salt_index <= 0:
            raise ValueError('salt_index should be greater than zero')
